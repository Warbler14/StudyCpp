{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${fileWorkspaceFolder}/objects/${fileBasenameNoExtension}.out",
            "args": [],
            "preLaunchTask": "g++ build active file",
            "stdio": [null,null,null], 
            "terminal": "integrated" 
        }
    ]
}
